import { ScrolldetectorDirective } from './scrolldetector.directive';

describe('ScrolldetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolldetectorDirective();
    expect(directive).toBeTruthy();
  });
});
